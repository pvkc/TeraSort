dist: trusty
sudo: false
language: cpp
compiler: gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5.2

script:
  - CXX=/usr/bin/g++-5.2 CC=/usr/bin/gcc-5 cmake .
  - cmake --build . -- -j2

